<%= form_for(@accounts_type) do |f| %>
  <% if @accounts_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accounts_type.errors.count, "error") %> prohibited this accounts_type from being saved:</h2>

      <ul>
      <% @accounts_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sign %><br />
    <%= f.number_field :sign %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
